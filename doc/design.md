# Z_COM 工具设计方案

## 1. 项目概述

### 1.1 项目背景
Z_COM 是嵌入式开发、硬件调试中常用的工具软件，用于通过串口与设备进行通信。本项目旨在开发一个功能完善、界面友好的串口调试工具，满足日常开发调试需求。

### 1.2 项目目标
- 提供稳定的串口通信功能
- 支持多种数据格式显示
- 提供数据日志和扩展发送功能
- 界面紧凑，适合工具软件使用场景

## 2. 功能需求

### 2.1 串口配置
- **串口选择**：显示可用串口列表，包括串口号和串口名称
- **波特率**：支持常见波特率（9600-921600）
- **数据位**：5, 6, 7, 8 位
- **停止位**：1, 1.5, 2 位
- **校验位**：None, Even, Odd, Mark, Space
- **流控制**：None, RTS/CTS, DTR/DSR, XON/XOFF
- **串口保护锁**：串口打开时禁用刷新按钮和串口选择，防止误操作
- **配置热更新**：修改波特率等参数后立即生效，无需关闭/重新打开串口

### 2.2 数据收发
- **发送格式**：ASCII 和 HEX
- **显示模式**：HEX、ASCII、HEX+ASCII 三种模式
- **自动滚动**：可控制接收区是否自动滚动
- **回车换行**：可选择发送时是否添加回车换行
- **自动发送**：支持定时自动发送，可设置间隔

### 2.3 数据日志
- **自动存储**：启动后自动记录所有收发数据
- **存储格式**：同时存储 ASCII 和 HEX 格式
- **日志文件**：按日期自动创建，存储在 `logs` 目录下
- **文件格式**：`YYYY-MM-DD.log`

### 2.4 扩展发送
- **数据管理**：支持添加、删除、清空、上移、下移数据条目
- **数据格式**：支持 HEX 和字符串两种格式
- **发送模式**：支持单条发送、多条顺序发送、循环发送
- **延时控制**：每条数据可设置独立延时（0-60000ms）
- **配置管理**：支持导入导出配置（JSON格式）

### 2.5 参数持久化
- **自动保存**：配置修改时自动保存
- **保存内容**：串口端口、波特率、显示模式、发送模式、自动滚动、自动发送间隔
- **配置文件**：存储在 `config/settings.json`
- **扩展发送配置**：存储在 `config/extended_send.json`

## 3. 界面设计

### 3.1 窗口布局
```
┌─────────────────────────────────────────────────────────────────┐
│ 菜单栏：文件 | 编辑 | 工具 | 帮助                                │
├─────────────────────────────────────────────────────────────────┤
│ 显示区域                                        │ 扩展发送区域   │
│ [HEX] [ASCII] [HEX+ASCII] [自动滚动] [清空]     │               │
│ ┌─────────────────────────────────────────────┐ │               │
│ │                                             │ │               │
│ │              接收数据显示区域                 │ │               │
│ │                                             │ │               │
│ └─────────────────────────────────────────────┘ │               │
├─────────────────────────────────────────────────┴───────────────┤
│ 发送区域                                                        │
│ ┌──────┐ ┌──────────────────────────────────────────────┐┌────┐ │
│ │      │ │ [ASCII] [HEX] [添加回车换行] [自动发送] [1000ms]││    │ │
│ │打开  │ ├──────────────────────────────────────────────┤│发  │ │
│ │串口  │ │                                              ││送  │ │
│ │      │ │ 发送输入框                                   ││    │ │
│ └──────┘ └──────────────────────────────────────────────┘└────┘ │
├─────────────────────────────────────────────────────────────────┤
│ 状态栏：刷新串口 | 串口选择 | 波特率 | 更多设置 | 扩展发送 | 状态信息│
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 组件说明

#### 3.2.1 显示区域
- **标题**：显示区域
- **工具栏**：HEX、ASCII、HEX+ASCII 单选按钮，自动滚动复选框，清空按钮
- **数据显示区**：QTextEdit 组件，只读，显示收发数据
- **颜色区分**：
  - 时间戳：淡青色 `#00CED1`
  - 箭头和标识：黑色加粗
  - 数据内容：黑色

#### 3.2.2 扩展发送区域
- **位置**：显示区域右侧
- **默认隐藏**：点击扩展发送按钮显示/隐藏
- **功能**：表格形式显示多条数据，支持批量发送

#### 3.2.3 发送区域
- **结构**：左中右三部分
  - **左边**：打开串口按钮（50px宽，方形，文字两行显示）
  - **中间**：两行控件（配置项:发送文本框 = 1:4）
    - 第一行：ASCII、HEX、添加回车换行、自动发送、间隔时间
    - 第二行：发送输入框
  - **右边**：发送按钮（50px宽，方形，文字两行显示）

#### 3.2.4 状态栏
- **左侧**：刷新串口、串口选择、波特率、更多设置、扩展发送
- **右侧**：串口状态、已发送字节、已接收字节
- **状态标签**：固定宽度150px，避免文本长度变化影响布局

### 3.3 窗口尺寸
- **默认大小**：890 x 685
- **最小大小**：可调整，各组件有最小尺寸限制

### 3.4 分割器布局
- **主分割器**（垂直）：上部分显示区域 + 下部分发送区域
- **顶部分割器**（水平）：左边显示区域 + 右边扩展发送区域
- **比例**：显示区域:发送区域 = 5:1，接收:扩展 = 1.55:1

## 4. 技术架构

### 4.1 技术选型
- **开发语言**：Python 3.8+
- **GUI 框架**：PySide6
- **串口通信**：pyserial
- **配置存储**：JSON 文件

### 4.2 项目结构
```
Z_COM/
├── config/                 # 配置目录
│   ├── settings.json      # 配置文件
│   └── extended_send.json # 扩展发送配置
├── doc/                    # 文档目录
│   └── design.md          # 设计方案文档
├── ui/                    # UI 文件目录
│   ├── main_window.ui     # 主窗口界面文件
│   ├── serial_settings_dialog.ui  # 串口设置对话框
│   ├── extended_send_widget.ui    # 扩展发送面板
│   ├── Ui_main_window.py
│   ├── Ui_serial_settings_dialog.py
│   └── Ui_extended_send_widget.py
├── src/                   # 源代码目录
│   ├── main.py           # 程序入口
│   ├── main_window.py    # 主窗口逻辑
│   ├── serial_settings_dialog.py  # 串口设置对话框
│   ├── extended_send_manager.py   # 扩展发送管理类
│   ├── extended_send_widget.py    # 扩展发送面板组件
│   ├── config_manager.py # 配置管理
│   └── __init__.py       # 包初始化
├── resources/            # 资源文件
│   └── icons/           # 图标文件
├── tests/               # 测试代码
├── logs/                # 日志目录
├── run.py               # 启动脚本
├── requirements.txt     # 依赖包
└── README.md           # 项目说明
```

### 4.3 核心类设计

#### SerialManager
- 管理串口通信
- 支持数据帧自动拼接（50ms超时）
- 支持配置热更新（reconfigure方法）
- 信号：data_received、connection_changed、error_occurred

#### DataHandler
- 数据格式转换
- HEX/ASCII 互转
- 格式化显示文本

#### Logger
- 日志管理
- 按日期创建日志文件
- 同时记录 ASCII 和 HEX

#### ExtendedSendManager
- 扩展发送数据管理
- 支持单条/多条/循环发送
- 配置导入导出

#### ExtendedSendWidget
- 扩展发送面板UI
- 表格显示数据条目
- 支持直接编辑

#### ConfigManager
- 配置管理
- 配置修改时自动保存

## 5. 使用说明

### 5.1 连接串口
1. 从状态栏串口下拉框选择串口
2. 选择波特率或点击更多设置配置详细参数
3. 点击发送区域的打开串口按钮

### 5.2 串口保护锁
- 串口打开后，刷新按钮和串口选择下拉框自动禁用
- 防止在通信过程中误操作导致连接中断
- 串口关闭后自动恢复可用状态

### 5.3 配置热更新
- 串口打开时可随时修改波特率、数据位、停止位、校验位等参数
- 修改后立即生效，无需手动关闭/重新打开串口
- 点击"更多设置"修改参数后自动应用到当前连接

### 5.4 接收数据
- 顶部选择显示模式：HEX、ASCII 或 HEX+ASCII
- 接收数据会自动显示在显示区域
- 支持自动滚动和清空

### 5.5 发送数据
- 发送区域选择格式：ASCII 或 HEX
- 在发送输入框输入数据
- 点击发送按钮发送
- 可选择添加回车换行

### 5.6 扩展发送
- 点击状态栏扩展发送按钮显示面板
- 点击添加按钮添加数据条目
- 直接在表格中编辑数据内容和注释
- 支持批量发送和循环发送

### 5.7 日志管理
- 所有收发数据自动记录到日志文件
- 日志文件按日期自动创建
- 存储在 `logs` 目录下

## 6. 编译说明

### 编译 UI 文件
```bash
pyside6-uic ui/main_window.ui -o ui/Ui_main_window.py
pyside6-uic ui/serial_settings_dialog.ui -o ui/Ui_serial_settings_dialog.py
pyside6-uic ui/extended_send_widget.ui -o ui/Ui_extended_send_widget.py
```

### 运行程序
```bash
python run.py
```

---

**文档版本**：v2.1
**最后更新**：2026年5月20日
